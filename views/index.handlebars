<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Mongo Web Scraper</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/saved">Saved Articles</a>
      </li>
      <a class="btn btn-primary btn-lg" href="#" role="button">Scrape for New Articles</a>
      <a class="btn btn-primary btn-lg" href="/saved" role="button">Clear Articles</a>
    </ul>
  </div>
</nav>

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Mongo Web Scraper</h1>
    <p class="lead">The Onion Edition</p>
  </div>
</div>

{{#each headline}}
{{#unless this.saved}}
<div class="card rounded">
    <div class="card-header">
        <a href='{{this.link}}'>{{this.title}}</a>
    </div>
    <div class="card-body">
        <p>{{this.summary}}</p>
        <br>
        <p class="card-text">click the button to save</p>
        <button data-id='{{this._id}}' class="btn btn-info save-article">Save Article</button>
    </div>
</div>
<br>
{{/unless}}
{{/each}}